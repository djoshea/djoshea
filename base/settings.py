from os.path import dirname, join

media_root = join(dirname(__file__), 'media')

media_root_img = join(media_root, 'img')
media_root_favicon = media_root_img
media_root_css = join(media_root, 'css')
media_root_js = join(media_root, 'js')
